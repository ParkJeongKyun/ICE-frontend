import { Avatar } from "antd";
import styled from "styled-components";

export const IceAvatar = styled(Avatar)`
  background-color: var(--container-bg-color);
  border: var(--main-border-size) solid var(--container-line-color);
`;
